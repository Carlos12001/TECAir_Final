/* The code `export interface AirAdmin` is defining an interface named `AirAdmin`. An interface in TypeScript is a way to define the structure of an object. In this case, the `AirAdmin` interface has two properties: `adminid` and `uemail`, both of which are of type `string`. This interface can be used to define objects that have these properties and their corresponding types. */
export interface AirAdmin {
  adminid: string;
  uemail: string;
}

/* The code `export const airAdmin = { adminid: '', uemail: '', };` is exporting a constant variable named `airAdmin`. This variable is an object with two properties: `adminid` and `uemail`, both of which are initially set to empty strings. */
export const airAdmin = {
  adminid: '',
  uemail: '',
};
