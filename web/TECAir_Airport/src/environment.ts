/* The code is exporting a constant object named `environment`. This object has two properties: `production` and `apiUrl`. */
export const environment = {
  production: false,
  apiUrl: 'https://localhost:7223/',
};
