/* The code is defining an interface named `Passenger` in TypeScript. An interface is a way to define the structure of an object. In this case, the `Passenger` interface has three properties: */
export interface Passenger {
  uemail: string;
  checkedin: boolean;
  fno: number;
}
